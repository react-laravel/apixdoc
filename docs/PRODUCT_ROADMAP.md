# ApiX Docs 产品迭代

目标：持续迭代为功能完整、可供团队日常使用的商业化 API 文档与调试产品。

交付方向已确认：兼顾私有部署与 SaaS，先补齐核心功能。以下阶段是实施顺序，不是缩小最终目标；只有核心流程完成并通过实际运行验证后，才能判定商业化目标完成。

## 参考产品

- [Postman 响应查看](https://learning.postman.com/docs/use/send-requests/response-data/responses)：查看、搜索、保存响应，展示状态、耗时、大小。
- [Hoppscotch 响应处理](https://docs.hoppscotch.io/documentation/getting-started/rest/response-handling)：原始响应、复制、下载、示例及结构化浏览。
- [cURL 参数语义](https://curl.se/docs/manpage.html)：导入时保留请求语义，不执行 Shell 或读取本地文件。
- [Apidog 调试与开发](https://docs.apidog.com/develop-and-debug-apis-in-apidog-541758m0)：文档、请求调试、响应校验构成完整流程。

## 迭代与验收

| 阶段 | 功能范围 | 完成证据 | 状态 |
| --- | --- | --- | --- |
| JSON 工作台 | 编辑高亮、行号、折叠、格式化、压缩、错误定位、结构树、查找、复制字段路径、下载；用于请求体、响应示例和在线响应 | 原始数值与字符串无损；大数据可浏览；非法 JSON 不被修改；浏览器桌面/手机操作验证 | 已完成本轮范围（2 MB / 200 层以内） |
| 请求调试 | 路径/查询/请求头参数、可配置认证、环境切换与变量、请求取消、响应统计、历史记录、cURL 复制与导入 | 使用回显服务验证最终 URL、headers、body；异常、取消及历史重放验证 | 核心流程已完成：环境/变量、路径参数、Bearer/Basic/API Key、表单编码、历史、cURL；高级传输继续迭代 |
| 文档与数据迁移 | OpenAPI 导入导出、Postman Collection 迁移、Markdown 描述、Schema、文档预览、公开/私有分享、接口复制与删除 | 常见规范样本往返导入导出；文档链接可独立打开；破损文件提供可操作错误 | 已完成文档页、规范导入、原始文件保留、同格式往返导出、复制/删除；高级编辑与格式转换继续迭代 |
| 团队协作 | 组织邀请与成员角色、只读访问、项目级权限、修改历史、冲突处理、搜索与导航持久化 | owner/admin/member/viewer 权限矩阵在 API 和 UI 一致；跨用户并发编辑和越权回归 | 已完成内容/配置/发布权限、邀请、成员管理、所有权转移与团队操作记录；已完成接口版本历史与编辑冲突处理；已完成项目设置并发保护和发布快照；完整商用审计及运维能力继续迭代 |
| 商用运维 | 账户与组织生命周期、配额/套餐基础、审计记录、监控与错误诊断、备份恢复、部署与升级、无障碍与性能基线 | 真实环境演练、恢复验证、版本交付记录；付费收款须有明确产品方案与商户配置 | 待实施 |

## 现有基础

- `824d1d0`：接口工作台布局、手机适配、保存状态、未保存修改提醒、取消拖拽修复、文件夹编辑、项目设置持久化。
- 该版本已有 154 项单元/组件测试和生产构建验证；这只证明已覆盖的功能，不代表商用产品完成。

## 每轮交付要求

1. 先追踪 UI、请求、权限与数据库的真实流程，再实现功能。
2. 针对数据正确性、状态恢复、回归风险添加有意义的自动化测试；验证桌面、手机和深色模式。
3. 使用独立本地测试数据，保留可复核的运行证据并清理临时数据。
4. 每轮记录已完成能力、未覆盖范围及下一阶段；不把未实现功能标为完成。


## 2026-09-12：JSON 工作台与响应查看

已落地：
- `/dashboard/tools/json` 独立工具，可导入文件、编辑、校验、美化、压缩、折叠、搜索、复制、下载和放大查看。
- JSON 结构树支持字段/值搜索、复制原值、复制 JSON Pointer；数组每批 100 项，批量展开最多五层/50 个分组。
- 请求体 Schema、请求示例、响应示例、在线响应复用工作台。JSON 处理基于源码 token，不将大整数、长小数或字符串转义转成 JavaScript 值再序列化。
- 在线测试新增响应大小、可编辑 Bearer Token、请求取消；修复空请求体回退到旧示例以及 URL 斜杠/查询参数拼接；切换编辑标签保留测试结果。

验证证据：
- 195 项测试通过，包含 JSON 精度/错误/边界、编辑工作流、代理取消和请求构造回归；TypeScript 及生产构建通过。lint 0 错误，5 项原有警告。
- 生产构建本地启动后，验证 50,000 项数组分页搜索、文件导入、美化撤销、路径复制、原文下载、错误定位、手机/深色界面；浏览器运行错误 0。
- 使用独立本地数据验证请求体保存、响应导入并保存、切换接口与刷新后的数值一致性。代理 UI 使用可控回包，服务端取消另由路由测试覆盖。
- 浏览器证据位于本地 `output/playwright/json-*.png` 与对应测试/构建日志；这些临时验证产物不进入源码提交。
- 生产模式验证按已有部署文档设置 `AUTH_TRUST_HOST=true`，并临时指定本地 `AUTH_URL`，没有修改线上配置。

该轮结束时尚未完成环境变量、请求历史和数据迁移等后续能力；进展见下方迭代记录。JSON 工作台完成不代表商业化总目标完成。


## 2026-09-12：环境化调试、认证、历史与 cURL

已落地：
- 调试器可选择/记住项目环境，使用默认环境；支持环境变量和临时变量覆盖、递归引用、循环/缺失变量提示。数值变量保留原始精度，JSON 字符串插值自动转义。
- 请求方法、地址、路径参数、查询参数、请求头和请求体均可临时调整。支持 OpenAPI `{id}` 和整段 `:id` 路径占位符，正确编码路径和查询值。
- Bearer、Basic 和 API Key（请求头/查询参数）认证；原始请求体和 URL 编码表单；1–60 秒超时、取消、响应状态/大小/耗时。
- 请求预览、实际发送和 cURL 使用同一份构造结果。导入的 cURL 与历史快照默认按原文发送，保留签名查询的编码以及字面量模板内容；需要时可重新启用占位符解析。
- 常用 cURL 参数支持引号、转义、续行、数据、请求头、Basic、查询、超时。未支持的选项、文件读取、Shell 运算和不兼容 GET/HEAD 请求体会明确报错；重定向行为差异在导入预览中显示。
- 历史按账号/项目/接口隔离，使用当前标签页存储，最多 20 条/1 MB；大响应保留前 64 KB 并明确标记，截断响应不能写回文档。载入历史不会自动发送。
- 文档默认值变化会更新仍在继承的字段，保留手动覆盖/删除；cURL 和历史请求不受文档默认值变更影响。账号或项目范围变化时丢弃旧的在途结果。
- JSON 变量模板支持高亮、美化、压缩、源位置校验和解析后结构预览，示例中的 `{{userId}}` 不再被误判为普通 JSON 错误。
- 环境/全局配置读取增加项目成员校验；公开项目详情不返回环境、全局请求头和全局参数。两个配置写接口复用设置校验和原子更新，避免部分更新清空其他配置。

验证证据：
- 256 项测试通过；lint 0 错误、5 项原有警告；生产构建和 TypeScript 通过。
- 真实 Postman Echo 请求验证了环境、路径/查询编码、继承请求头、JSON 插值、Basic、API Key 和 URL 编码表单。未转发应用登录 Cookie；导出 cURL 再导入后的请求内容一致。
- 浏览器验证了刷新后的历史载入、精确重放、JSON 模板美化、共享默认值即时更新与临时修改保留；移动端/深色模式无页面横向溢出。
- 生产模式新账号验收：浏览器运行错误 0；模拟缺少 `crypto.randomUUID` 时仍能创建参数行和历史记录。
- 真实非成员账号访问环境和全局配置得到 403；公开项目详情仍返回 200，运行配置数组为空。
- 本地证据：`output/playwright/request-*.png`、测试/构建日志和验收脚本。独立验收数据已清理，证据不提交到源码。

下一阶段与未完成范围：
1. OpenAPI / Postman Collection 导入导出、接口复制/删除和可独立访问的文档预览。
2. 发布前补齐完整角色权限矩阵（包括 viewer 写权限）、公开内容中的认证信息处理、邀请和修改历史/冲突处理。
3. Multipart/文件上传、OAuth、高级响应与请求历史能力、私有网络访问的部署级策略继续迭代。
4. 配额/套餐基础、账户生命周期、备份恢复、审计与监控、部署升级演练仍未完成。当前代理继续限制公网 HTTP/HTTPS；不把已完成的核心调试能力等同于商业化总目标完成。
5. 运行中观察到 pg 8 驱动的并发查询弃用提示；当前版本通过验证，升级 pg 9 前需核对 Prisma 适配器兼容性。


## 2026-09-12：独立文档、只读权限与原生格式导出

已落地：
- `/docs/{projectId}?endpoint={endpointId}` 独立文档页，包含目录搜索、接口链接、Markdown 表格/代码、参数、请求头、请求/响应结构与 JSON 示例。支持手机阅读和深色模式。
- 私有文档要求登录并校验组织成员关系，登录后回到原接口；公开文档允许匿名访问。取消公开后，新文档访问需要登录，匿名导出被拒绝。
- 项目权限集中判断：owner/admin 管理发布和项目删除；member 可编辑接口、目录和运行配置但不能变更公开状态；viewer 只能查看文档，不能读取运行凭据或调用写接口。前端同步隐藏/禁用对应操作。
- 公开/只读 DTO 隐藏环境与全局配置，清理 URL 中的已识别认证信息，并对结构化认证示例（含模板、无效 JSON 的可识别字段）脱敏。Markdown 原始 HTML 不执行，危险链接不可点击。
- 原生项目可导出 OpenAPI 3.1 JSON/YAML 和 Postman Collection 2.1，保留当前模型中的参数、结构、响应及精确数值。Authorization 使用 OpenAPI securitySchemes，Postman 路径变量转换为 `:id`。
- 复制接口保留参数、请求头、请求体、响应结构；删除需要确认。修复跨项目目录引用与直接 API 创建目录循环的问题，读取项目目录时不混入历史异常关联的其他项目接口。

验证证据：
- 299 项测试通过；TypeScript、生产构建通过；lint 0 错误、5 项原有警告。
- 使用真实 owner/member/viewer 账号和匿名访问验证私有登录回跳、只读 UI 与写入 403、普通成员配置修改、管理员发布/撤回、匿名文档和安全导出。
- 浏览器实际复制并删除接口，校验关联参数/请求体/响应结构；下载三种格式并检查内容与大整数精度。
- 公开 HTML 与导出内容未出现验收凭据；损坏的跨项目目录关联未泄漏其他项目接口；Markdown 注入未执行，移动端无页面横向溢出。
- 验证产物保留于本地 `output/playwright/docs-*`，不提交测试账号或导出内容。

未完成与下一步：
- OpenAPI/Postman 导入尚未实现。本轮先完成了发布依赖的权限与查看链路。导入前需要扩展规范存储，保留 `$ref`、组合 Schema、枚举、多媒体类型、非数字响应码等信息，不能只导入路径而丢弃原始定义。
- 当前导出反映现有原生数据模型；重复方法/路径、含查询字符串的 OpenAPI 路径、无效结构会提供错误，复杂规范的完整往返验收留待导入实现后完成。
- 邀请、成员角色调整/所有权转移、修改历史、并发编辑冲突、发布快照和商用运维仍在总目标内，尚未完成。
- 自由文本的内容由文档作者负责；结构化字段脱敏并不等于任意文字中的秘密都可自动识别。


## 2026-09-12：规范导入、复杂结构保留与冲突保护

已落地：
- OpenAPI 3.0 / 3.1（JSON、YAML）和 Postman Collection 2.1 文件上传或粘贴导入。支持预览、重复方法/路径提示、追加跳过，以及管理员确认项目名称后的整体替换。
- 保存原始文件、接口定义和编辑基准。私有原始文件可下载；内部同格式导出合并当前修改，保留未改动的引用、组合结构、枚举、命名示例、多个媒体类型、默认/范围响应、扩展字段与集合脚本。删除接口不会在导出时重新出现，移动目录同步到导出结果。
- 规范定义、版本、集合全局配置或变量有冲突时，在预览阶段报错。保存使用数据库事务和预览指纹，绑定项目内容、上传文件与导入模式；过期预览和并发提交返回 409，失败时不留下部分数据。
- 参数可编辑完整 Schema；请求体可切换媒体类型并分别保存结构与示例；响应支持 `default`、`2XX` 和无响应体。复制接口保留新增结构字段和来源关联。
- 导入的服务器、路径参数、集合认证与变量连接到在线测试。接口默认变量独立于原有项目默认环境，项目环境可显式覆盖；可选添加导入环境，保留原有环境并避免重名。
- 原始文件与来源信息只对编辑成员开放。公开/只读文档保留经过处理的数据模型与引用，隐藏原始配置与已识别认证示例；OpenAPI 3.0 安全导出保留原有版本语义。
- 增量数据库迁移增加规范来源、参数结构、多媒体请求体和响应状态字段。已有部署流程会在构建前执行迁移。

验证证据：
- 317 项测试通过，生产构建与数据库结构校验通过。包括 JSON/YAML 精确数值、别名/循环/重复键、复杂规范往返、编辑与删除、目录移动、权限、文件变更与过期预览、集合认证和变量继承。lint 0 错误、5 项原有警告。
- 本地生产模式实际验证文件上传、预览、保存、三接口追加/替换、请求体 JSON/XML 切换后保存、原始文件与导出文件下载。Postman 下载内容与原始集合语义一致。
- 实际并发提交同一预览得到 200 与 409；损坏规范和过期预览不会改变项目内容。替换后原有环境保留。
- 匿名文档与模型库返回 200；匿名导入返回 401；viewer 导入和原始文件访问返回 403。公开数据与导出没有包含验收凭据、来源定义或编辑基准。
- 使用可控代理回包验证导入的集合地址、路径参数和 Bearer 认证进入实际请求载荷；集合脚本未执行。本轮未向外部服务发送集合认证信息。
- 独立验收组织与两个账号已清理，数据库恢复到原有 1 个账号 / 1 个项目；临时浏览器和服务已关闭。
- 手机预览与替换确认无页面横向溢出，修复后公开文档和集合调试的浏览器运行错误为 0。验证产物位于本地 `output/playwright/import-*`，不进入源码提交。

边界与后续：
- 单文件最大 2 MB、最多 2000 个接口、展开后的接口数据最大 8 MB。外部引用仅保留，不自动加载；集合脚本、文件上传模式、回调、Webhooks 和复杂参数序列化会给出保留或调试限制提示。
- 原始文件是完整档案；跨格式导出与公开导出使用可映射的文档字段，不能当作完整集合配置的迁移副本。高级字段的专用编辑器、外部引用解析、完整规范校验和更多格式转换继续迭代。
- 团队邀请、角色调整/所有权转移、一般编辑的版本历史与冲突处理、发布快照，以及配额、审计、监控和恢复演练仍未完成，商业化总目标保持进行中。


## 2026-09-12：团队邀请、成员管理与所有权

已落地：
- 管理员生成绑定邮箱的邀请链接，有效期 7 天，可撤销或重新生成。密钥只返回一次，数据库保存摘要；打开链接不会自动加入。链接由管理员复制分发，本轮没有发送邮件。
- 已有账号登录后确认加入；新用户在邀请页设置姓名与密码，创建普通账号并自动登录。错误账号、已使用/过期/撤销链接有明确反馈。邀请密钥保留在浏览器链接片段中，不进入请求 URL；登录回跳不依赖临时存储。
- 所有者可以管理管理员、编辑成员和只读成员；管理员只能邀请和管理编辑/只读成员。角色调整、移除、退出和所有权转移均在服务端重新检查权限。
- 所有权转移要求确认接任者邮箱，原所有者成为管理员。组织至少保留一名所有者；旧版本留下的多所有者组织仍可正常退出。成员被移除或权限调整后，其待处理邀请失效。
- 团队写入使用组织事务锁与版本号，避免过期页面覆盖新状态；邀请接受、账号创建、成员加入、邀请消费和操作记录作为同一事务提交。
- 成员页新增搜索、角色操作、待处理邀请、最近 50 条团队记录和手机布局。成员顺序稳定，普通成员不接收管理员专用的邀请和操作记录。
- 登录会话按当前账号检查身份与平台角色，已删除账号的旧会话不能继续调用后台。平台删除用户也不能绕过组织所有者保护；仍关联已创建项目/接口的账号暂不能硬删除。

验证证据：
- 328 项自动化测试通过；另有可重复执行的本地数据库集成检查 `npm run test:team`，覆盖角色层级、邮箱绑定、链接时效、撤销/更新、注册竞争、过期版本、降权撤销与所有权竞争，并自动清理自己的数据。
- 本地生产模式验证了生成/撤销邀请、调整成员角色、已有账号登录回跳、新账号注册与自动登录、只读 UI、退出组织及所有权转移。邀请网络请求 URL 未包含密钥；手机页面没有横向溢出。
- 退出后访问组织返回 403；原所有者不能再次转移所有权；平台删除仍为所有者的账号返回 409。账号删除后旧会话失效，后续 API 返回 401。
- 临时组织、账号与邀请均已清理，数据库恢复到原有 1 个账号、1 个组织、1 个项目；浏览器和测试服务已关闭。
- 构建与类型检查通过；lint 无错误，保留 5 项原有警告。浏览器与本地验收证据保存在 `output/playwright/team-*`，不提交账号、密钥或截图。

边界与后续：
- [团队接口说明](TEAM_COLLABORATION.md) 记录版本号、邀请与角色接口。旧成员 POST 入口现在生成邀请，不能直接把用户加入组织；接入方应使用新的邀请流程。
- 本轮提供团队操作记录；接口内容的版本历史、编辑冲突、发布快照、自动邮件投递、账号恢复/软删除、SSO 和完整商用审计仍需继续实现。
- 商业化总目标继续进行，配额/套餐、监控、备份恢复和部署升级演练尚未完成。


## 2026-09-12：接口版本历史、编辑冲突与回收站

已落地：
- 内容保存携带接口版本号，完整保存关联参数、请求头、请求体与响应；每次实际变更记录操作者和时间，保留最近 50 个版本。旧接口首次修改前记录基线，相同内容不重复创建版本。
- 非重叠字段自动合并；同字段冲突展示编辑前、自己的修改和当前内容，逐项确认后再保存。列表不按行号猜测合并，旧基线失效时要求重新确认。
- 编辑器为各分区保留独立基线，保存一处分区不会丢掉其他草稿。请求体结构与示例可分别合并，JSON 大整数和媒体内容保持精度。
- 版本历史支持差异查看、导出单个历史版本、恢复并创建新版本。新增接口请求头编辑与移除请求体操作。
- 删除改为回收站，支持搜索、分页和选择历史版本恢复。普通项目读取、文档预览、统计和导出排除已删除接口。
- 导入替换归档旧接口和来源文件；恢复时验证定义兼容性，并只恢复所选接口。原目录消失时恢复到根目录并提示。
- 目录修改、批量排序和相关接口历史在同一事务提交，使用独立目录版本号；拒绝循环、跨项目关联和通过排序修改项目归属。项目切换隔离编辑状态，手机操作区避免挤压项目标题。

验证证据：
- 336 项测试通过；`npm run test:documents` 覆盖真实数据库中的权限、三方合并、并发冲突、列表处理、结构/示例精度、历史恢复、目录隔离、50 版本保留上限及旧规范重新启用。
- 两个实际编辑账号验证了名称/描述合并、未保存分区保留、结构/示例分别合并、同字段冲突选择及新版本写入。
- 浏览器验证历史导出与恢复、回收站恢复及删除确认；请求头和大整数示例与所选版本一致。
- 连续替换规范后，活动导出没有混入旧来源；恢复旧接口重新启用所需模型，未恢复同来源的其他已删除接口；历史导出只使用选中版本的来源。
- viewer 访问历史/回收站返回 403，匿名历史请求返回 401；删除接口不再通过正常只读详情返回，公开数据中未出现历史凭据。
- 生产构建、类型与数据库结构校验通过；lint 无错误，保留 5 项原有警告。最终手机恢复流程无页面横向溢出，浏览器运行错误为 0。
- 测试账号、项目、组织和版本记录已清理，数据库恢复到原有 1 个账号、1 个项目、1 个组织，测试浏览器及服务已关闭。
- 浏览器记录位于本地 `output/playwright/history-*`，不提交测试凭据、导出内容或截图。接口与迁移协议见 [版本功能说明](DOCUMENT_HISTORY.md)。

后续仍在总目标中：项目设置的并发保护、独立发布快照与版本化分享、完整操作审计、配额/套餐、自动邮件、账号生命周期、监控及备份恢复演练。当前接口版本能力不代表商业化总目标已完成。


## 2026-09-12：项目配置并发保护与发布快照

已落地：
- 设置使用独立版本号和 20 个合并基线，非重叠修改合并，同字段冲突要求确认；窗口保留草稿，未保存关闭会提醒。环境导入同步配置版本，读取版本与配置使用一致快照。
- 发布保存完整的文档快照，内部编辑、删除和项目更名不影响现有发布。发布前检查内容指纹和访问范围；一般成员不能发布或改变访问范围。
- 文档入口指向当前发布，固定链接指向具体版本。页面复制链接及下载均绑定所见版本，公开 API 和导出不会绕过快照读取草稿。
- 支持切换历史发布、撤销指定版本和停止所有版本分享；全部撤回后旧链接不会在重新发布时恢复。编辑成员仍可内部查看和下载撤回记录。
- 发布内容排除环境、全局凭据、原始来源和编辑历史；访问权限实时检查，改为团队可见后所有版本均受成员约束。
- 已有公开与团队私有文档在升级后保留首次基线，避免旧链接直接变为不可用；新项目需要显式发布。预览失败不阻止撤回已有发布。

验证证据：
- 338 项测试通过；发布数据库检查覆盖内容冻结、固定版本、访问范围变化、预览过期、撤销后不复活、升级竞争、私有基线、设置合并、导入环境保留及错误预览下的撤回。
- 两个实际账号验证设置自动合并与冲突选择；浏览器完成发布、固定版本下载、历史切换和停止分享。切换历史发布不修改工作区草稿。
- 匿名查看页面、项目/接口 API 和导出在工作区修改后保持原版本；已识别凭据及内部发布说明未出现在公开数据中，大整数精度保留。
- 撤回版本的内部预览和下载已验证，匿名访问被拒绝；重新发布后旧链接仍返回 404。
- 生产构建、类型与数据库结构校验通过；lint 无错误，保留 5 项原有警告。测试账号、发布记录及项目均已清理，数据库恢复到原有 1 个账号、1 个组织、1 个项目，测试浏览器和服务已关闭。
- 本地验收文件位于 `output/playwright/publication-*`，不提交测试凭据或截图。协议见 [项目设置与发布说明](PROJECT_PUBLICATIONS.md)。

后续仍需完成：完整操作审计、配额/套餐、自动邮件、账号恢复/软删除、SSO、监控与备份恢复演练，以及大项目性能基线。商业化总目标继续进行。


## 2026-09-12：统一审计、健康探针与部署保障

已落地：
- 统一记录成功提交的关键业务变更，与业务事务一起提交；元数据白名单排除请求体、配置值、示例及邀请密钥。记录保留对象名称与范围，项目/组织删除后仍能追查。
- 操作记录页面支持组织、项目、类型、日期和关键词筛选，游标分页及 CSV 导出；组织管理员与平台管理员的可见范围分离，普通成员不能读取审计。
- 新增默认预览的过期记录维护命令，显式执行才清理，按批写入清理摘要。发布审计使用实际发布序号，避免混淆并发状态版本。
- 存活与就绪探针分离，检查数据库、迁移和必要数据表；并发探针合并、短期缓存及超时限制。平台管理员可查看运行状态，公共响应不暴露配置细节。
- 服务器未知错误带问题编号，结构化日志不输出原始异常消息和请求内容。部署验证就绪状态，保留 3 个应用版本，排除本地环境及测试文件。
- 去掉默认管理员密码和部署自动 seed；首次初始化必须明确提供凭据，已有账号与密码保持不变。

验证证据：
- 348 项测试通过，审计数据库检查覆盖事务回滚、越权拒绝、敏感字段排除、分页、删除保留与清理预览。
- 真实组织管理员完成筛选和 CSV 下载；项目删除后仍能查看删除记录，组织删除后平台管理员仍能追查，而原成员访问被拒绝。
- 平台运行状态页显示真实数据库与迁移结果，手机布局无页面横向溢出；普通成员访问运行详情返回 403。
- 独立故障实例使用不可用数据库地址，8 个并发就绪请求均返回 503，存活检查仍返回 200，公共响应没有泄露诊断细节；故障日志仅生成一次带编号的安全记录。
- 生产构建与类型检查通过，审计、文档、团队及发布数据库回归全部通过，部署脚本语法检查通过；lint 无错误，保留 5 项原有警告。数据库回归仍有 pg 驱动并发查询弃用提示，后续升级驱动前需处理。
- 测试数据已清理，数据库恢复到原有 1 个账号、1 个组织、1 个项目，测试浏览器与服务均已关闭。本轮未执行生产部署。
- 验收产物保留在本地 `output/playwright/audit-*`、`operations-*`。操作协议与维护说明见 [审计及运行保障](AUDIT_AND_OPERATIONS.md)。

后续仍在总目标内：配额/套餐、账号恢复与软删除、自动邮件、SSO、备份恢复演练、告警集成及大项目性能基线。本轮成功操作审计不代表已覆盖所有读取与失败请求。
